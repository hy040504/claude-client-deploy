# claude-client-deploy

Claude.ai CLI Client 배포용 저장소입니다.

## 🚀 설치 및 사용법

1. `npm install` 로 의존성 설치
2. `.env.example` 을 `.env` 로 복사 후 정보 입력
3. `node background-login.js` 로 로그인
4. `npm run chat-prompt` 로 채팅 시작
